// ===== i18n =====
const I18N = {
  'zh-CN': {
    skip: '跳到主要内容',
    navHow: '使用流程', navFeatures: '功能特点', navScreenshots: '应用截图', navFaq: '常见问题', navGet: '获取 App',
    heroEyebrow: '✦ 基于《原子习惯》',
    heroTitle: '科学养成好习惯',
    heroSubtitle: '从身份认同出发，用「提示 → 渴望 → 行动 → 奖励」的习惯循环，帮你建立并坚持真正长久的好习惯。',
    downloadBtn: 'App Store 下载', learnMore: '了解更多',
    trust1: '🔒 数据全本地', trust2: '📶 离线可用', trust3: '🌏 中英双语',
    hbToday: '今天', hbStreak: '连续 12 天',
    identityLabel: '我的身份', identityValue: '我是一个自律的人',
    loopKicker: '习惯回路', loopCenter: '习惯循环',
    loopCue: '提示', loopCraving: '渴望', loopAction: '行动', loopReward: '奖励',
    hbDone: '今日完成',
    chipA: '🔥 连续 12 天', chipB: '✅ 今日 3/4',
    proofRating: '习惯养成 · 效率工具',
    stat1: '原生体验', stat2: '本地存储', stat3: '双语界面', stat4: '广告 · 追踪',
    howTitle: '简单三步，开始改变', howSub: '先想清楚你想成为谁，剩下的每天做一点。',
    step1Title: '定义身份', step1Desc: '设定「我是一个……的人」的身份目标，比如「我是一个热爱运动的人」。',
    step2Title: '设计习惯', step2Desc: '创建习惯，配置提示、行动与奖励，搭起属于自己的习惯循环。',
    step3Title: '持续追踪', step3Desc: '每日打卡、专注番茄钟，累积连续天数，看着进度不断刷新。',
    featuresTitle: '强大功能，助您成功', featuresSub: '为长期坚持而设计的每一个细节。',
    feature1Title: '身份驱动', feature1Desc: '基于「我是……」的身份认同，从根本上重塑行为模式，让改变由内而外发生。',
    feature2Title: '习惯循环', feature2Desc: '设置提示、渴望、行动与奖励，构建完整的习惯回路，让好习惯自动被触发。',
    feature3Title: '番茄钟专注', feature3Desc: '内置番茄钟与习惯联动，专注时长自动记录，让坚持看得见、更有动力。',
    feature4Title: '数据分析', feature4Desc: '可视化追踪进度，智能分析你的坚持规律，给出更贴合你的改进建议。',
    feature5Title: '连续记录', feature5Desc: '激励性的连续天数记录，一天天累积成就感，不断挑战自己的最佳纪录。',
    feature6Title: '中英双语', feature6Desc: '中英文界面随时一键切换，满足不同用户的使用习惯与语言偏好。',
    screenshotsTitle: '应用界面展示', screenshotsSub: '左右滑动，点击可放大查看。',
    shot1: '今日习惯一览', shot2: '创建身份与习惯', shot3: '进度与数据统计', shot4: '番茄钟专注计时',
    privacyTitle: '您的习惯，只属于您', privacyDesc: '没有账号，没有云端，没有追踪。所有习惯数据都保存在设备本地，卸载即彻底清除。',
    pp1: '数据本地存储', pp2: '离线即可使用', pp3: '无任何追踪', pp4: '无广告干扰',
    faqTitle: '常见问题',
    q1: 'WeiRabits 需要联网吗？', a1: '不需要。所有功能均可离线使用，您的习惯数据全部保存在设备本地，不会离开手机。',
    q2: '数据会同步到云端吗？', a2: '不会。数据只保存在您的设备本地，我们不上传、不收集、不追踪任何习惯信息。',
    q3: '什么是身份驱动的习惯养成？', a3: '从「我是一个……的人」出发设定身份，让每一次打卡都强化这个身份，习惯因此更容易长期坚持。',
    q4: '习惯循环是怎么运作的？', a4: '为习惯设置提示、渴望、行动与奖励，形成完整回路，让好习惯自然而然被触发和重复。',
    q5: '番茄钟如何与习惯结合？', a5: '可为需要专注的习惯启动内置番茄钟，专注时长会自动计入统计，帮助你看到坚持的成果。',
    q6: '支持哪些语言？', a6: '完整支持简体中文与英文界面，可随时一键切换。',
    downloadTitle: '立即开始您的习惯养成之旅', downloadSubtitle: '完全免费，无广告，注重隐私保护。', downloadNote: '免费下载 · 无广告 · 尊重隐私',
    footerRights: 'WeiProduct. 保留所有权利。', footerMadeBy: 'Built by WeiProduct', privacyPolicy: '隐私政策',
    stickySub: '习惯养成 · 免费', stickyGet: '获取'
  },
  'en': {
    skip: 'Skip to content',
    navHow: 'How it works', navFeatures: 'Features', navScreenshots: 'Screenshots', navFaq: 'FAQ', navGet: 'Get App',
    heroEyebrow: '✦ Powered by Atomic Habits',
    heroTitle: 'Build Habits That Stick',
    heroSubtitle: 'Start from who you want to be, then let the Cue → Craving → Routine → Reward loop help you build habits that actually last.',
    downloadBtn: 'Download on App Store', learnMore: 'Learn more',
    trust1: '🔒 Fully on-device', trust2: '📶 Works offline', trust3: '🌏 EN / 中文',
    hbToday: 'Today', hbStreak: '12-day streak',
    identityLabel: 'My identity', identityValue: 'I am a disciplined person',
    loopKicker: 'The habit loop', loopCenter: 'Habit Loop',
    loopCue: 'Cue', loopCraving: 'Crave', loopAction: 'Routine', loopReward: 'Reward',
    hbDone: 'Done today',
    chipA: '🔥 12-day streak', chipB: '✅ 3/4 today',
    proofRating: 'Habit building · Productivity',
    stat1: 'Native feel', stat2: 'On-device', stat3: 'Bilingual', stat4: 'Ads · Tracking',
    howTitle: 'Start changing in three steps', howSub: 'Get clear on who you want to be, then do a little every day.',
    step1Title: 'Define your identity', step1Desc: 'Set an identity goal like "I am a person who loves to move," and let habits serve it.',
    step2Title: 'Design the habit', step2Desc: 'Create a habit and configure its cue, routine and reward to form your own habit loop.',
    step3Title: 'Track every day', step3Desc: 'Check in daily, focus with the Pomodoro timer, build streaks and watch your progress climb.',
    featuresTitle: 'Powerful features for success', featuresSub: 'Every detail designed to help you stick with it.',
    feature1Title: 'Identity-Driven', feature1Desc: 'Built on an "I am…" identity so behavior changes from the inside out, not just willpower.',
    feature2Title: 'Habit Loop', feature2Desc: 'Set cue, craving, routine and reward to form a complete loop that triggers good habits automatically.',
    feature3Title: 'Pomodoro Focus', feature3Desc: 'A built-in Pomodoro timer links to your habits and auto-logs focus time, making effort visible.',
    feature4Title: 'Analytics', feature4Desc: 'Visual progress tracking with smart analysis of your patterns and tailored suggestions to improve.',
    feature5Title: 'Streaks', feature5Desc: 'Motivating day-by-day streaks build momentum and push you to beat your personal best.',
    feature6Title: 'Bilingual', feature6Desc: 'Switch between Chinese and English interfaces in one tap to fit your language and habits.',
    screenshotsTitle: 'App Screenshots', screenshotsSub: 'Swipe through — tap any shot to zoom in.',
    shot1: "Today's habits at a glance", shot2: 'Create identities & habits', shot3: 'Progress & statistics', shot4: 'Pomodoro focus timer',
    privacyTitle: 'Your habits are yours alone', privacyDesc: 'No account, no cloud, no tracking. All habit data stays on your device and is wiped completely on uninstall.',
    pp1: 'Stored on-device', pp2: 'Works offline', pp3: 'Zero tracking', pp4: 'No ads',
    faqTitle: 'Frequently Asked Questions',
    q1: 'Does WeiRabits need internet?', a1: 'No. Every feature works offline, and all your habit data stays on your device — it never leaves your phone.',
    q2: 'Is my data synced to the cloud?', a2: 'No. Data stays only on your device. We never upload, collect, or track any habit information.',
    q3: 'What is identity-driven habit building?', a3: 'You start from "I am a person who…," so every check-in reinforces that identity — which makes habits far easier to keep long term.',
    q4: 'How does the habit loop work?', a4: 'Set a cue, craving, routine and reward for each habit to form a complete loop, so good habits get triggered and repeated naturally.',
    q5: 'How does the Pomodoro timer fit in?', a5: 'Start the built-in Pomodoro timer for habits that need focus; the focused time is logged into your stats so you can see your effort pay off.',
    q6: 'Which languages are supported?', a6: 'Full support for both Simplified Chinese and English interfaces, switchable anytime with one tap.',
    downloadTitle: 'Start building better habits today', downloadSubtitle: 'Completely free, no ads, privacy-first.', downloadNote: 'Free download · No ads · Privacy respected',
    footerRights: 'WeiProduct. All rights reserved.', footerMadeBy: 'Built by WeiProduct', privacyPolicy: 'Privacy Policy',
    stickySub: 'Habit building · Free', stickyGet: 'Get'
  }
};

let currentLang = 'zh-CN';

function applyLang(lang) {
  currentLang = I18N[lang] ? lang : 'zh-CN';
  const t = I18N[currentLang];
  document.querySelectorAll('[data-i18n]').forEach(el => {
    const k = el.getAttribute('data-i18n');
    if (t[k] !== undefined) el.textContent = t[k];
  });
  document.documentElement.lang = currentLang;
  const ls = document.getElementById('langSwitch');
  if (ls) ls.textContent = currentLang === 'zh-CN' ? 'EN' : '中文';
  try { localStorage.setItem('lang', currentLang); } catch (e) {}
}

function initLang() {
  let saved;
  try { saved = localStorage.getItem('lang'); } catch (e) {}
  if (!saved) saved = (navigator.language || '').toLowerCase().startsWith('zh') ? 'zh-CN' : 'en';
  applyLang(saved);
}

// ===== Theme =====
function setTheme(theme) {
  document.documentElement.setAttribute('data-theme', theme);
  const meta = document.getElementById('themeColorMeta');
  if (meta) meta.setAttribute('content', theme === 'dark' ? '#0B0B0F' : '#007AFF');
  try { localStorage.setItem('theme', theme); } catch (e) {}
}
function initTheme() {
  let saved;
  try { saved = localStorage.getItem('theme'); } catch (e) {}
  if (!saved) saved = window.matchMedia('(prefers-color-scheme: dark)').matches ? 'dark' : 'light';
  setTheme(saved);
}

// ===== Gallery =====
function initGallery() {
  const track = document.getElementById('galTrack');
  if (!track) return;
  const shots = Array.from(track.children);
  const dotsWrap = document.getElementById('galDots');
  const prev = document.getElementById('galPrev');
  const next = document.getElementById('galNext');

  shots.forEach((_, i) => {
    const b = document.createElement('button');
    b.type = 'button';
    b.setAttribute('aria-label', 'screenshot ' + (i + 1));
    if (i === 0) b.classList.add('active');
    b.addEventListener('click', () => shots[i].scrollIntoView({ behavior: 'smooth', inline: 'center', block: 'nearest' }));
    dotsWrap.appendChild(b);
  });
  const dots = Array.from(dotsWrap.children);

  function activeIndex() {
    const c = track.scrollLeft + track.clientWidth / 2;
    let best = 0, bd = Infinity;
    shots.forEach((s, i) => {
      const center = s.offsetLeft + s.offsetWidth / 2;
      const d = Math.abs(center - c);
      if (d < bd) { bd = d; best = i; }
    });
    return best;
  }
  track.addEventListener('scroll', () => {
    const i = activeIndex();
    dots.forEach((d, j) => d.classList.toggle('active', j === i));
  }, { passive: true });

  const step = () => (shots[1] ? shots[1].offsetLeft - shots[0].offsetLeft : 300);
  if (prev) prev.addEventListener('click', () => track.scrollBy({ left: -step(), behavior: 'smooth' }));
  if (next) next.addEventListener('click', () => track.scrollBy({ left: step(), behavior: 'smooth' }));

  // Lightbox
  const lb = document.createElement('div');
  lb.className = 'lightbox';
  lb.innerHTML = '<button class="lightbox-close" aria-label="close">&times;</button><img alt="">';
  document.body.appendChild(lb);
  const lbImg = lb.querySelector('img');
  const close = () => lb.classList.remove('open');
  lb.addEventListener('click', e => { if (e.target === lb || e.target.classList.contains('lightbox-close')) close(); });
  document.addEventListener('keydown', e => { if (e.key === 'Escape') close(); });
  track.querySelectorAll('.phone').forEach(p => {
    p.addEventListener('click', () => {
      const img = p.querySelector('img');
      lbImg.src = img.src; lbImg.alt = img.alt;
      lb.classList.add('open');
    });
  });
}

// ===== Scroll reveal =====
function initReveal() {
  const els = document.querySelectorAll('.reveal');
  if (!('IntersectionObserver' in window)) { els.forEach(e => e.classList.add('in')); return; }
  const io = new IntersectionObserver((entries) => {
    entries.forEach(en => {
      if (en.isIntersecting) { en.target.classList.add('in'); io.unobserve(en.target); }
    });
  }, { threshold: 0.12, rootMargin: '0px 0px -40px 0px' });
  els.forEach((el, i) => { el.style.transitionDelay = (Math.min(i, 6) * 0.05) + 's'; io.observe(el); });
}

// ===== Nav + sticky =====
function initScroll() {
  const nav = document.getElementById('navbar');
  const sticky = document.getElementById('stickyCta');
  const onScroll = () => {
    const y = window.scrollY;
    if (nav) nav.classList.toggle('scrolled', y > 20);
    if (sticky) sticky.classList.toggle('show', y > 620);
  };
  window.addEventListener('scroll', onScroll, { passive: true });
  onScroll();
}

// ===== Init =====
document.addEventListener('DOMContentLoaded', () => {
  initTheme();
  initLang();
  initGallery();
  initReveal();
  initScroll();

  const yr = document.getElementById('currentYear');
  if (yr) yr.textContent = new Date().getFullYear();

  const ls = document.getElementById('langSwitch');
  if (ls) ls.addEventListener('click', () => applyLang(currentLang === 'zh-CN' ? 'en' : 'zh-CN'));
  const tt = document.getElementById('themeToggle');
  if (tt) tt.addEventListener('click', () => setTheme(document.documentElement.getAttribute('data-theme') === 'dark' ? 'light' : 'dark'));

  document.querySelectorAll('a[href^="#"]').forEach(a => {
    a.addEventListener('click', function (e) {
      const id = this.getAttribute('href');
      if (id.length > 1) {
        const target = document.querySelector(id);
        if (target) { e.preventDefault(); target.scrollIntoView({ behavior: 'smooth', block: 'start' }); }
      }
    });
  });
});
